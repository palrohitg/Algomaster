// Dublicate parenthesis
/*

    - (, operands and operator tho push push 
    - if ) and then s.top() ( then return ture
    - else pop unitill (

*/


#include<iostream>
#include<string>
#include<stack>
using namespace std;

int main() {

    int t; 
    cin >> t; 
    while(t--) {
        string str;
        cin >> str;
        duplicateBrackets(arr, n);
    }
    return 0;
}