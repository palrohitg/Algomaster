/*
  * is basically a deference operator what will will gives u value of any address
  Example : 
    int *a;  a will contain the addrss of the bucket.
  	a = &b;  here a contain the address of bucket
    *a then it returns the value at particular buckets
    
*/

#include<iostream>
using namespace std;

int main() {

	// cout << INT_MIN;
	// cout << INT_MAX;
	cout << "Kadane Algorithm" << endl;
	return 0;
}

