#include <bits/stdc++.h>
using namespace std;

int main()
{


    // Base case if 0 and 1 is the prime number
    if (n <= 1)
        flag = 0;

    int num = 6;

    int ans = sqrt(num);

    cout << ans << endl;

    return 0;
}