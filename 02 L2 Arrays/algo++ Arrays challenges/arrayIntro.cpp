/*
  All the elements are initialize to zeroes all the another will take the garbage mistake on the better to have to zeroes on the array
*/



#include<iostream>
using namespace std; 

int main() {
	int arr[10];

	for (int i = 0; i <10 ; ++i)
	{
		cout << arr[i] << " ,";
	}

	return 0;
}