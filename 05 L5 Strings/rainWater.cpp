// Rain Water Trapping

// for each element = min(leftMostHighest, rightMostHeighest) - currentHeights


#include<bits/stdc++.h>
using namespace std;

int main() {

// #ifndef ONLINE_JUDGE
// 	freopen("../input.txt", "r", stdin);
// 	freopen("../output.txt", "w", stdout);
// #endif

	vector<int> height;

	height.push_back(10);
	height.push_back(20);
	height.push_back(20);

	cout << height.size() << endl;

	int n = height.size();

	cout << n << endl;

	return 0;
}