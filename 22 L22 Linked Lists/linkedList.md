# Operation and Complexity in linked list


```cpp
1. Basic implementation of linked list
2.

```



* Deletion take O(1) time because we just need to change the pointer
* they can be accessed randomly like arrays(which have index)