#include<bits/stdc++.h>
using namespace std; 


class Node {
public:
    int data; 
    Node *next; 

    Node(int) {
        this->data = data; 
        this->next = NULL; 
    }
}; 


void insertAtTail(Node *&head, int data) {
    if (head == NULL) {
        head = new Node(data); 
        return; 
    }
    return; 
}


int main() {

    Node *head = NULL; 
    int data; cin >> data;

    return 0; 
}