// Return the highest frequency 
// make the hashtable key is the elements and count is the values 

#include<bits/stdc++.h> 
using namespace std; 


int main() {


    int n; cin >> n; 
    unordered_map<int> 
    int *arr = new int[n]; 

    for (int i = 0; i < n; i++) {
        int data; cin >> data; 
        arr[i] = data; 
    }

    
    

    return 0;
}