// heaps based questions 
#include<bits/stdc++.h> 
using namespace std; 

int main() {

    int n; 
    cin >> n; 

    int *arr = new int[n]; 

    // take the input from the users 
    for (int i = 0; i < n; i++) {
        int data; cin >> data; 
        arr[i] = data; 
    }

    int k; cin >> k; 
    

    return 0; 
}