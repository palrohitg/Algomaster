package main

/*
	Element struct {
		int Value;
		int ArrayIndex;
		int ElementIndex;

		Element(int v, int ai, int ei): value(v1),
		ArrayIndex(ai), ElementIndex(ei) {}

		bool operator > (const Element &others) const {
			return Value > others.Value;
		}

		priority_queue<Element,vector<Element>greater<Element>>minHeap;
	};


*/
