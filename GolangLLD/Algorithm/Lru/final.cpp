#include<bits/stdc++.h>
using namespace std;

int main() {
    cout << "First elements need to check for the new data" << endl;  
    list<int> dll; 

    dll.push_back(10); 
    dll.push_back(20); 

    cout << dll.back(); 


    
    return 0;
}