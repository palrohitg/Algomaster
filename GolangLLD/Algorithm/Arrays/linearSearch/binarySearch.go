package main

import (
	"fmt"
	"math"
)

// Endors labs to figure it not dependencies
// To find the Open-Source Processes it helps to understand the internally to find
// To Projects to Score Cards is the difference things to find the open-sources
// Staff Engineer try to find the software architectures find the process becomes open -ssh

// Merge two Sorted arrays
// Merge both the array in O(N) time and then find the median by : n-1 + n / 2 :
// This will be have the median of the arrays
// Important Questions
/*
	todo:
		1. Median of two sorted arrays.
		2. Allocate minimum no of pages.
		3.
		4.

int countStundents(int arr[], int pages) {
	students = 1;
	pagesCount = 0;

	for (int i = 0; i < n; i++) {
		if (pages+ arr[i] <= pagesCount) {
			pagesStudent += arr[i]
		}
		else {
			student += 1
			pagesCount = arr[i];
		}
  }
  return students;s
}



*/

func main() {
	fmt.Println(math.MinInt)
	fmt.Println(math.MaxInt)
	return
}
