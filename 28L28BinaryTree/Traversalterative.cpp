/*
	PreOrder Traveral Iterative :
	1. Create an empty stack and push the root elements
	2. while stack is not empty :
		a. pop out the top elements and print the elements
		b. push the right element child to stack (just poped out elements)
		c. push the left element child to stack (just poped out elements)

	Inorder Traversal Iterative :
	1. Create an empty stack and push the root element
	2. while (!s.empty() and cur != NULL)
		a. push the left child into the stacks
		b. pop out and points to right elements

	3. Questions
*/