## 1. Word Pattern Easy 
```cpp 
example  : pattern : abba , string : dog cat cat dog 
output : Pattern is matching here.  

- Count of the size string and pattern is not equal then false.  P.size() != word.size() 
- 
``` 

```go 
part := strings.SplitN(externalId, "-", 2)
``` 


```cpp 
class Solution {
public:
    vector<string> getWordList(string s) {

        return ""
    }

    bool wordPattern(string pattern, string s) {
        vector<string> wordList:
        
    }
};
``` 

